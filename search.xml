<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>CMU 15-445 Fall 2021 Project0: C++ Primer</title>
    <url>/2022/01/02/CMU-15-445-Fall-2021-Project0-C-Primer/</url>
    <content><![CDATA[<h1 id="Summary"><a href="#Summary" class="headerlink" title="Summary"></a>Summary</h1><p>第0个项目是熟悉C++，涉及模板，OO设计，unique_ptr，支持使用C++17特性。</p>
<p>涉及的核心点：</p>
<p><strong>使用flatten array来表示二维数组</strong>，使用一维数组来抽象表示二维数组可以提高CPU访存效率，最大程度应用上CPU L1，L2，L3 Cache。</p>
<p>我觉得这是这个项目的核心点，其次就是C++ OOP需要注意的一些点，比如构造析构，array new&#x2F; array delete，exception，template等。这些并不是难点，属于C++基础部分。</p>
<h1 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h1><h2 id="Matrix-constructor-x2F-destructor"><a href="#Matrix-constructor-x2F-destructor" class="headerlink" title="Matrix constructor&#x2F;destructor"></a>Matrix constructor&#x2F;destructor</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Matrix</span>(<span class="type">int</span> rows, <span class="type">int</span> cols) &#123;</span><br><span class="line">   rows_ = rows;</span><br><span class="line">   cols_ = cols;</span><br><span class="line">   linear_ = <span class="keyword">new</span> T[rows * cols];  <span class="comment">// key point is flattened array</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">virtual</span> ~<span class="built_in">Matrix</span>() &#123; <span class="keyword">delete</span>[] linear_; &#125;</span><br></pre></td></tr></table></figure>

<h2 id="Matrix-get-x2F-set"><a href="#Matrix-get-x2F-set" class="headerlink" title="Matrix get&#x2F;set"></a>Matrix get&#x2F;set</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">int</span> <span class="title">GetRowCount</span><span class="params">()</span> <span class="type">const</span> </span>&#123; <span class="keyword">return</span> rows_; &#125;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">int</span> <span class="title">GetColumnCount</span><span class="params">()</span> <span class="type">const</span> </span>&#123; <span class="keyword">return</span> cols_; &#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">virtual</span> T <span class="title">GetElement</span><span class="params">(<span class="type">int</span> i, <span class="type">int</span> j)</span> <span class="type">const</span> </span>&#123;</span><br><span class="line">   <span class="keyword">if</span> (i &lt; <span class="number">0</span> || i &gt;= rows_ || j &lt; <span class="number">0</span> || j &gt;= cols_) &#123;</span><br><span class="line">     <span class="keyword">throw</span> <span class="built_in">Exception</span>(ExceptionType::OUT_OF_RANGE, <span class="string">&quot;Invalid arguments: out of range.&quot;</span>);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">return</span> <span class="keyword">this</span>-&gt;linear_[i * cols_ + j];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">SetElement</span><span class="params">(<span class="type">int</span> i, <span class="type">int</span> j, T val)</span> </span>&#123;</span><br><span class="line">   <span class="keyword">if</span> (i &lt; <span class="number">0</span> || i &gt;= rows_ || j &lt; <span class="number">0</span> || j &gt;= cols_) &#123;</span><br><span class="line">     <span class="keyword">throw</span> <span class="built_in">Exception</span>(ExceptionType::OUT_OF_RANGE, <span class="string">&quot;Invalid arguments: out of range&quot;</span>);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">this</span>-&gt;linear_[i * cols_ + j] = val;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Matrix-Fill-Data"><a href="#Matrix-Fill-Data" class="headerlink" title="Matrix Fill Data"></a>Matrix Fill Data</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">FillFrom</span><span class="params">(<span class="type">const</span> std::vector&lt;T&gt; &amp;source)</span> </span>&#123;</span><br><span class="line">   <span class="type">size_t</span> size = rows_ * cols_;</span><br><span class="line">   <span class="keyword">if</span> (source.<span class="built_in">size</span>() != size) &#123;</span><br><span class="line">     <span class="keyword">throw</span> <span class="built_in">Exception</span>(ExceptionType::OUT_OF_RANGE, <span class="string">&quot;Invalid arguments: size not fit&quot;</span>);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">for</span> (<span class="type">size_t</span> i = <span class="number">0</span>; i &lt; source.<span class="built_in">size</span>(); ++i) &#123;</span><br><span class="line">     <span class="keyword">this</span>-&gt;linear_[i] = source[i];</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="RowMatrix-constructor-x2F-destructor"><a href="#RowMatrix-constructor-x2F-destructor" class="headerlink" title="RowMatrix constructor&#x2F;destructor"></a>RowMatrix constructor&#x2F;destructor</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">RowMatrix</span>(<span class="type">int</span> rows, <span class="type">int</span> cols) : <span class="built_in">Matrix</span>&lt;T&gt;(rows, cols) &#123;</span><br><span class="line">  data_ = <span class="keyword">new</span> T *[rows];</span><br><span class="line">  <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; rows; ++i) &#123;</span><br><span class="line">    data_[i] = Matrix&lt;T&gt;::linear_ + i * cols;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">~<span class="built_in">RowMatrix</span>() <span class="keyword">override</span> &#123; <span class="keyword">delete</span>[] data_; &#125;</span><br></pre></td></tr></table></figure>

<h2 id="RowMatrix-get-x2F-set"><a href="#RowMatrix-get-x2F-set" class="headerlink" title="RowMatrix get&#x2F;set"></a>RowMatrix get&#x2F;set</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">GetRowCount</span><span class="params">()</span> <span class="type">const</span> <span class="keyword">override</span> </span>&#123; <span class="keyword">return</span> <span class="keyword">this</span>-&gt;rows_; &#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">GetColumnCount</span><span class="params">()</span> <span class="type">const</span> <span class="keyword">override</span> </span>&#123; <span class="keyword">return</span> <span class="keyword">this</span>-&gt;cols_; &#125;</span><br><span class="line"></span><br><span class="line"><span class="function">T <span class="title">GetElement</span><span class="params">(<span class="type">int</span> i, <span class="type">int</span> j)</span> <span class="type">const</span> <span class="keyword">override</span> </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (i &lt; <span class="number">0</span> || i &gt;= <span class="keyword">this</span>-&gt;rows_ || j &lt; <span class="number">0</span> || j &gt;= <span class="keyword">this</span>-&gt;cols_) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="built_in">Exception</span>(ExceptionType::OUT_OF_RANGE, <span class="string">&quot;Invalid arguments: out of range&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> data_[i][j];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">SetElement</span><span class="params">(<span class="type">int</span> i, <span class="type">int</span> j, T val)</span> <span class="keyword">override</span> </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (i &lt; <span class="number">0</span> || i &gt;= <span class="keyword">this</span>-&gt;rows_ || j &lt; <span class="number">0</span> || j &gt;= <span class="keyword">this</span>-&gt;cols_) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="built_in">Exception</span>(ExceptionType::OUT_OF_RANGE, <span class="string">&quot;Invalid arguments: out of range&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  data_[i][j] = val;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="RowMatrix-Fill-Data"><a href="#RowMatrix-Fill-Data" class="headerlink" title="RowMatrix Fill Data"></a>RowMatrix Fill Data</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">FillFrom</span><span class="params">(<span class="type">const</span> std::vector&lt;T&gt; &amp;source)</span> <span class="keyword">override</span> </span>&#123;</span><br><span class="line">  <span class="type">size_t</span> size = <span class="keyword">this</span>-&gt;rows_ * <span class="keyword">this</span>-&gt;cols_;</span><br><span class="line">  <span class="keyword">if</span> (source.<span class="built_in">size</span>() != size) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="built_in">Exception</span>(ExceptionType::OUT_OF_RANGE, <span class="string">&quot;Invalid arguments: size not fit&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">for</span> (<span class="type">size_t</span> i = <span class="number">0</span>; i &lt; source.<span class="built_in">size</span>(); ++i) &#123;</span><br><span class="line">    <span class="keyword">this</span>-&gt;linear_[i] = source[i];</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="RowMatrixOperations-ADD"><a href="#RowMatrixOperations-ADD" class="headerlink" title="RowMatrixOperations ADD"></a>RowMatrixOperations ADD</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">static</span> std::unique_ptr&lt;RowMatrix&lt;T&gt;&gt; <span class="built_in">Add</span>(<span class="type">const</span> RowMatrix&lt;T&gt; *matrixA,</span><br><span class="line">                                          <span class="type">const</span> RowMatrix&lt;T&gt; *matrixB) &#123;</span><br><span class="line">  <span class="type">int</span> arow = matrixA-&gt;<span class="built_in">GetRowCount</span>();</span><br><span class="line">  <span class="type">int</span> acol = matrixA-&gt;<span class="built_in">GetColumnCount</span>();</span><br><span class="line"></span><br><span class="line">  <span class="type">int</span> brow = matrixB-&gt;<span class="built_in">GetRowCount</span>();</span><br><span class="line">  <span class="type">int</span> bcol = matrixB-&gt;<span class="built_in">GetColumnCount</span>();</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (arow == brow &amp;&amp; acol == bcol) &#123;</span><br><span class="line">    std::unique_ptr&lt;RowMatrix&lt;T&gt;&gt; res =</span><br><span class="line">        std::make_unique&lt;RowMatrix&lt;T&gt;&gt;(arow, acol);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; arow; ++i) &#123;</span><br><span class="line">      <span class="keyword">for</span> (<span class="type">int</span> j = <span class="number">0</span>; j &lt; acol; ++j) &#123;</span><br><span class="line">        res-&gt;<span class="built_in">SetElement</span>(</span><br><span class="line">            i, j, matrixA-&gt;<span class="built_in">GetElement</span>(i, j) + matrixB-&gt;<span class="built_in">GetElement</span>(i, j));</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> res;</span><br><span class="line">  &#125;</span><br><span class="line">    <span class="keyword">return</span> std::unique_ptr&lt;RowMatrix&lt;T&gt;&gt;(<span class="literal">nullptr</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="RowMatrixOperations-Multiply"><a href="#RowMatrixOperations-Multiply" class="headerlink" title="RowMatrixOperations Multiply"></a>RowMatrixOperations Multiply</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">static</span> std::unique_ptr&lt;RowMatrix&lt;T&gt;&gt; <span class="built_in">Multiply</span>(<span class="type">const</span> RowMatrix&lt;T&gt; *matrixA,</span><br><span class="line">                                              <span class="type">const</span> RowMatrix&lt;T&gt; *matrixB) &#123;</span><br><span class="line">  <span class="type">int</span> arow = matrixA-&gt;<span class="built_in">GetRowCount</span>();</span><br><span class="line">  <span class="type">int</span> acol = matrixA-&gt;<span class="built_in">GetColumnCount</span>();</span><br><span class="line"></span><br><span class="line">  <span class="type">int</span> brow = matrixB-&gt;<span class="built_in">GetRowCount</span>();</span><br><span class="line">  <span class="type">int</span> bcol = matrixB-&gt;<span class="built_in">GetColumnCount</span>();</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (acol == brow) &#123;</span><br><span class="line">    std::unique_ptr&lt;RowMatrix&lt;T&gt;&gt; res =</span><br><span class="line">        std::make_unique&lt;RowMatrix&lt;T&gt;&gt;(arow, bcol);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; arow; ++i) &#123;</span><br><span class="line">      <span class="keyword">for</span> (<span class="type">int</span> j = <span class="number">0</span>; j &lt; bcol; ++j) &#123;</span><br><span class="line">        T val = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> k = <span class="number">0</span>; k &lt; acol; ++k) &#123;</span><br><span class="line">          val += matrixA-&gt;<span class="built_in">GetElement</span>(i, k) * matrixB-&gt;<span class="built_in">GetElement</span>(k, j);</span><br><span class="line">        &#125;</span><br><span class="line">        res-&gt;<span class="built_in">SetElement</span>(i, j, val);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> res;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> std::unique_ptr&lt;RowMatrix&lt;T&gt;&gt;(<span class="literal">nullptr</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="RowMatrixOperations-GEMM"><a href="#RowMatrixOperations-GEMM" class="headerlink" title="RowMatrixOperations GEMM"></a>RowMatrixOperations GEMM</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">static</span> std::unique_ptr&lt;RowMatrix&lt;T&gt;&gt; <span class="built_in">GEMM</span>(<span class="type">const</span> RowMatrix&lt;T&gt; *matrixA,</span><br><span class="line">                                          <span class="type">const</span> RowMatrix&lt;T&gt; *matrixB,</span><br><span class="line">                                          <span class="type">const</span> RowMatrix&lt;T&gt; *matrixC) &#123;</span><br><span class="line">  <span class="comment">// TODO(P0): Add implementation</span></span><br><span class="line">  <span class="type">int</span> arow = matrixA-&gt;<span class="built_in">GetRowCount</span>();</span><br><span class="line">  <span class="type">int</span> acol = matrixA-&gt;<span class="built_in">GetColumnCount</span>();</span><br><span class="line"></span><br><span class="line">  <span class="type">int</span> brow = matrixB-&gt;<span class="built_in">GetRowCount</span>();</span><br><span class="line">  <span class="type">int</span> bcol = matrixB-&gt;<span class="built_in">GetColumnCount</span>();</span><br><span class="line"></span><br><span class="line">  <span class="type">int</span> crow = matrixC-&gt;<span class="built_in">GetRowCount</span>();</span><br><span class="line">  <span class="type">int</span> ccol = matrixC-&gt;<span class="built_in">GetColumnCount</span>();</span><br><span class="line"></span><br><span class="line">  <span class="comment">// A*B时，A的列必须等于B的行</span></span><br><span class="line">  <span class="keyword">if</span> (acol != brow) &#123;</span><br><span class="line">    <span class="keyword">return</span> std::unique_ptr&lt;RowMatrix&lt;T&gt;&gt;(<span class="literal">nullptr</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// res和 C进行加法，行列必须完全对应</span></span><br><span class="line">  <span class="comment">// A(n*m) * B(p*m) = res(n*p),所以检查原矩阵的对应关系即可</span></span><br><span class="line">  <span class="keyword">if</span> (arow != crow || bcol != ccol) &#123;</span><br><span class="line">    <span class="keyword">return</span> std::unique_ptr&lt;RowMatrix&lt;T&gt;&gt;(<span class="literal">nullptr</span>);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  std::unique_ptr&lt;RowMatrix&lt;T&gt;&gt; res = <span class="built_in">Multiply</span>(matrixA, matrixB);</span><br><span class="line">  std::unique_ptr&lt;RowMatrix&lt;T&gt;&gt; ret = <span class="built_in">Add</span>(res.<span class="built_in">get</span>(), matrixC);</span><br><span class="line">  <span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="Googletest"><a href="#Googletest" class="headerlink" title="Googletest"></a>Googletest</h1><p><img  
                     lazyload
                     src="/images/loading.svg"
                     data-src="/../img/cmu15445_p0_gtest.png"
                      alt="gtest"
                ></p>
<h1 id="Gradescope"><a href="#Gradescope" class="headerlink" title="Gradescope"></a>Gradescope</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">make format</span><br><span class="line">make check-lint</span><br><span class="line">make check-clang-tidy</span><br><span class="line"><span class="built_in">cd</span> ..</span><br><span class="line">zip project0-submission.zip src/include/primer/p0_starter.h</span><br></pre></td></tr></table></figure>

<p><img  
                     lazyload
                     src="/images/loading.svg"
                     data-src="/../img/cmu15445_p0_gradescope.png"
                      alt="gradescope"
                ></p>
]]></content>
      <categories>
        <category>cmu15445</category>
      </categories>
      <tags>
        <tag>cmu15445</tag>
        <tag>project0</tag>
        <tag>c++ Primer</tag>
      </tags>
  </entry>
  <entry>
    <title>CMU 15-445 Fall 2021 Project1: Buffer Pool</title>
    <url>/2022/01/14/CMU-15-445-Fall-2021-Project1-Buffer-Pool/</url>
    <content><![CDATA[<h1 id="Summary"><a href="#Summary" class="headerlink" title="Summary"></a>Summary</h1><p>第1个项目是实现<code>Buffer Pool Manager</code>，<code>BufferPoolManager</code> 负责在磁盘和内存之间搬运物理页面的，如果页面被标记为脏，那么需要写回磁盘，保证数据不丢失，如果<code>Buffer Pool</code> 满了，那么需要从内存总换出页面到磁盘上，空出空闲位置给新的页面请求。实现在内存和磁盘之间搬运数据，对请求者透明。 </p>
<p>Buffer Pool 项目的实现包括三个部分</p>
<ul>
<li><strong>Task 1: LRU Replacement Policy</strong></li>
<li><strong>Task 2:  Buffer Pool Manager Instance</strong></li>
<li><strong>Task 3: Parallel Buffer Pool Manager</strong></li>
</ul>
<p>分别是实现LRU页面替换策略[1]，实现Buffer Pool Manager 的行为[2]（保证线程安全性，意味着Task2的实现必须加锁保护，否则通不过并发测试的），Task 3 主要是分配多个bufferpoolmanager 来减少锁竞争的[3]。</p>
<h1 id="坑点"><a href="#坑点" class="headerlink" title="坑点"></a>坑点</h1><p>1、LRUReplacer::Unpin( )的时候是<strong>不需要将page调到LRU开头的</strong>，原地不动就好</p>
<p>2、BufferPoolManagerInstance::NewPgImp( ) newpage的时候，如果空闲页是从LRU返回的page，那么证明此page是被复用了，此时需要检查页面的is_dirty 标志位，<strong>如果为脏，需要写回磁盘，否则新页面一覆盖，这部分数据就丢失了。</strong></p>
<p>3、BufferPoolManagerInstance::UnpinPgImp( ) 当<strong>所有线程都不 pin this Page时候, 就可以交给 LRU调度了</strong>，否则就内存泄露了，这个页没法再被复用</p>
<p>4、BufferPoolManagerInstance::UnpinPgImp( ) 的传入参数 <code>is_dirty</code>为true的时候再标记内存页，<strong>无脑标记是不可以的</strong>，可能覆盖掉原先页面的标记，导致数据丢失，因为你不标记，但是其他线程可能标记了，所有线程都能标记页面的</p>
<h1 id="Task-1-LRU-Replacement-Policy"><a href="#Task-1-LRU-Replacement-Policy" class="headerlink" title="Task 1: LRU Replacement Policy"></a>Task 1: LRU Replacement Policy</h1><h2 id="lru-replacer-h"><a href="#lru-replacer-h" class="headerlink" title="lru_replacer.h"></a>lru_replacer.h</h2><p>LRU数据结构的组成就是 “list + hashmap” ，这两种数据结构的组合可以提供 O(1)时间复杂度的 <code>add/remove/find</code> 操作。C++里可以使用标准库的<code>std::list</code> 和 <code>std::unordered_map</code>来实现LRU。</p>
<p>LRU 算法可以参考小林coding的这篇文章——<a class="link"   href="https://mp.weixin.qq.com/s/2pzGEmcSD_Gpjrnz3_J4zQ" >手撕 LRU 算法（更正版）<i class="fas fa-external-link-alt"></i></a></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">size_t</span> capacity_;                 <span class="comment">// 容量</span></span><br><span class="line">std::list&lt;<span class="type">frame_id_t</span>&gt; list_;	 <span class="comment">// 提供 O(1) add/remove操作的list</span></span><br><span class="line">std::unordered_map&lt;<span class="type">frame_id_t</span>, std::list&lt;<span class="type">frame_id_t</span>&gt;::iterator&gt; hashmap_;	<span class="comment">// 提供O(1)查找操作的hashmap</span></span><br><span class="line">std::mutex latch_;               <span class="comment">// 加锁</span></span><br></pre></td></tr></table></figure>


<h2 id="lru-replacer-cpp"><a href="#lru-replacer-cpp" class="headerlink" title="lru_replacer.cpp"></a>lru_replacer.cpp</h2><p>接下来是lru_replacer的实现部分，</p>
<ul>
<li>Victim——向LRU请求一个内存页，如果没有空闲页，则失败</li>
<li>Pin—表示将内存页pin在内存里，不可被LRU调度，所以将内存页踢出<code>list_</code>，这样就不会被LRU调度了</li>
<li>Unpin——表明可被 LRU调度, 加入 <code>list_</code>，(注意：如果内存页已经处于被LRU管理中，那么不需要将此页掉到<code>list_</code>开头，此处的行为常说的LRU算法不太一样)</li>
<li>Size——返回LRU管理的内存页的数目</li>
</ul>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">LRUReplacer::<span class="built_in">LRUReplacer</span>(<span class="type">size_t</span> num_pages) &#123; capacity_ = num_pages; &#125;</span><br><span class="line">LRUReplacer::~<span class="built_in">LRUReplacer</span>() = <span class="keyword">default</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">LRUReplacer::Victim</span><span class="params">(<span class="type">frame_id_t</span> *frame_id)</span> </span>&#123;</span><br><span class="line">  <span class="function">std::lock_guard&lt;std::mutex&gt; <span class="title">lkguard</span><span class="params">(latch_)</span></span>;</span><br><span class="line">  <span class="keyword">if</span> (!list_.<span class="built_in">empty</span>()) &#123;</span><br><span class="line">    *frame_id = list_.<span class="built_in">back</span>();</span><br><span class="line">    <span class="comment">// 调出的Page需要清空 list_ 和 hashmap_</span></span><br><span class="line">    list_.<span class="built_in">pop_back</span>();</span><br><span class="line">    <span class="keyword">auto</span> iter = hashmap_.<span class="built_in">find</span>(*frame_id);</span><br><span class="line">    hashmap_.<span class="built_in">erase</span>(iter);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  frame_id = <span class="literal">nullptr</span>;</span><br><span class="line">  <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Pin表明不可被 LRU调度, 踢出 list_</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">LRUReplacer::Pin</span><span class="params">(<span class="type">frame_id_t</span> frame_id)</span> </span>&#123;</span><br><span class="line">  <span class="function">std::lock_guard&lt;std::mutex&gt; <span class="title">lkguard</span><span class="params">(latch_)</span></span>;</span><br><span class="line">    </span><br><span class="line">  <span class="keyword">auto</span> iter = hashmap_.<span class="built_in">find</span>(frame_id);</span><br><span class="line">  <span class="keyword">if</span> (iter != hashmap_.<span class="built_in">end</span>()) &#123;</span><br><span class="line">    list_.<span class="built_in">erase</span>(iter-&gt;second);</span><br><span class="line">    hashmap_.<span class="built_in">erase</span>(iter);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Unpin表明可被 LRU调度, 加入 list_</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">LRUReplacer::Unpin</span><span class="params">(<span class="type">frame_id_t</span> frame_id)</span> </span>&#123;</span><br><span class="line">  <span class="function">std::lock_guard&lt;std::mutex&gt; <span class="title">lkguard</span><span class="params">(latch_)</span></span>;</span><br><span class="line">  <span class="comment">// LRU中已有的数据，不需要调到开头，否则通过不了gtest 测试</span></span><br><span class="line">  <span class="keyword">auto</span> iter = hashmap_.<span class="built_in">find</span>(frame_id);</span><br><span class="line">  <span class="keyword">if</span> (iter != hashmap_.<span class="built_in">end</span>()) &#123;</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// add to list_&#x27;s head and hashmap_</span></span><br><span class="line">  list_.<span class="built_in">push_front</span>(frame_id);</span><br><span class="line">  hashmap_[frame_id] = list_.<span class="built_in">begin</span>();</span><br><span class="line">  <span class="comment">// if size &gt; capacity, remove other nodes</span></span><br><span class="line">  <span class="keyword">if</span> (list_.<span class="built_in">size</span>() &gt; capacity_) &#123;</span><br><span class="line">    <span class="type">frame_id_t</span> id = list_.<span class="built_in">back</span>();</span><br><span class="line">    list_.<span class="built_in">pop_back</span>();</span><br><span class="line">    hashmap_.<span class="built_in">erase</span>(id);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">size_t</span> <span class="title">LRUReplacer::Size</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> list_.<span class="built_in">size</span>(); &#125;</span><br></pre></td></tr></table></figure>


<h1 id="Task-2-Buffer-Pool-Manager-Instance"><a href="#Task-2-Buffer-Pool-Manager-Instance" class="headerlink" title="Task 2:  Buffer Pool Manager Instance"></a>Task 2:  Buffer Pool Manager Instance</h1><h2 id="buffer-pool-manager-instance-cpp"><a href="#buffer-pool-manager-instance-cpp" class="headerlink" title="buffer_pool_manager_instance.cpp"></a>buffer_pool_manager_instance.cpp</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 刷盘，将内存页写回磁盘进行持久化</span></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">BufferPoolManagerInstance::FlushPgImp</span><span class="params">(<span class="type">page_id_t</span> page_id)</span> </span>&#123;</span><br><span class="line">  <span class="function">std::lock_guard&lt;std::mutex&gt; <span class="title">lkguard</span><span class="params">(latch_)</span></span>;</span><br><span class="line">  <span class="keyword">auto</span> iter = page_table_.<span class="built_in">find</span>(page_id);</span><br><span class="line">  <span class="keyword">if</span> (page_id == INVALID_PAGE_ID || iter == page_table_.<span class="built_in">end</span>()) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// frame_id 用来定位内存中的 Page所处位置的, 类似一个 slot用来放 page的</span></span><br><span class="line">  <span class="keyword">auto</span> frame_id = iter-&gt;second;</span><br><span class="line">  disk_manager_-&gt;<span class="built_in">WritePage</span>(pages_[frame_id].page_id_, pages_[frame_id].data_);</span><br><span class="line">  <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">BufferPoolManagerInstance::FlushAllPgsImp</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="function">std::lock_guard&lt;std::mutex&gt; <span class="title">lkguard</span><span class="params">(latch_)</span></span>;</span><br><span class="line">  <span class="keyword">for</span> (<span class="type">size_t</span> i = <span class="number">0</span>; i &lt; pool_size_; ++i) &#123;</span><br><span class="line">    <span class="built_in">FlushPgImp</span>(i);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 在内存里新建一个内存页，获取顺序为 free_list_获取 -&gt; LRU获取 -&gt; 失败</span></span><br><span class="line"><span class="function">Page *<span class="title">BufferPoolManagerInstance::NewPgImp</span><span class="params">(<span class="type">page_id_t</span> *page_id)</span> </span>&#123;</span><br><span class="line">  <span class="function">std::lock_guard&lt;std::mutex&gt; <span class="title">lkguard</span><span class="params">(latch_)</span></span>;</span><br><span class="line">  <span class="type">frame_id_t</span> frame_id;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (!free_list_.<span class="built_in">empty</span>()) &#123;</span><br><span class="line">    frame_id = free_list_.<span class="built_in">front</span>();</span><br><span class="line">    free_list_.<span class="built_in">pop_front</span>();</span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> (replacer_-&gt;<span class="built_in">Size</span>() &gt; <span class="number">0</span>) &#123;</span><br><span class="line">    replacer_-&gt;<span class="built_in">Victim</span>(&amp;frame_id);</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 重中之重，复用之前一定要将 dirty的页面写回磁盘，否则数据就丢失了</span></span><br><span class="line">  <span class="comment">// 也无法通过 test2</span></span><br><span class="line">  <span class="keyword">auto</span> *prev_page = &amp;pages_[frame_id];</span><br><span class="line">  <span class="keyword">if</span> (prev_page-&gt;<span class="built_in">IsDirty</span>()) &#123;</span><br><span class="line">    disk_manager_-&gt;<span class="built_in">WritePage</span>(prev_page-&gt;page_id_, prev_page-&gt;data_);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 现在再分配page_id,等到有空闲 Page再分配，不要写在函数一开头，这样更符合逻辑</span></span><br><span class="line">  *page_id = <span class="built_in">AllocatePage</span>();</span><br><span class="line">  <span class="comment">// 更新 page_table_</span></span><br><span class="line">  page_table_.<span class="built_in">erase</span>(prev_page-&gt;page_id_);   <span class="comment">// remove old record</span></span><br><span class="line">  page_table_.<span class="built_in">emplace</span>(*page_id, frame_id);  <span class="comment">// add new record</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// Modify &quot;reuse page&quot; metadata and data(must)</span></span><br><span class="line">  <span class="comment">// cautain: pin_count reset to 1</span></span><br><span class="line">  pages_[frame_id].pin_count_ = <span class="number">1</span>;</span><br><span class="line">  pages_[frame_id].page_id_ = *page_id;</span><br><span class="line">  pages_[frame_id].is_dirty_ = <span class="literal">false</span>;</span><br><span class="line">  pages_[frame_id].<span class="built_in">ResetMemory</span>();</span><br><span class="line"></span><br><span class="line">  <span class="keyword">auto</span> *page = &amp;pages_[frame_id];</span><br><span class="line">  <span class="keyword">return</span> page;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Request Page</span></span><br><span class="line"><span class="comment">// 如果请求的页面 P在内存中立即返回，如果不在，就调入内存</span></span><br><span class="line"><span class="function">Page *<span class="title">BufferPoolManagerInstance::FetchPgImp</span><span class="params">(<span class="type">page_id_t</span> page_id)</span> </span>&#123;</span><br><span class="line">  <span class="function">std::lock_guard&lt;std::mutex&gt; <span class="title">lkguard</span><span class="params">(latch_)</span></span>;</span><br><span class="line">  <span class="comment">// exists, pin and return</span></span><br><span class="line">  <span class="keyword">auto</span> iter = page_table_.<span class="built_in">find</span>(page_id);</span><br><span class="line">  <span class="keyword">if</span> (iter != page_table_.<span class="built_in">end</span>()) &#123;</span><br><span class="line">    <span class="keyword">auto</span> frame_id = iter-&gt;second;</span><br><span class="line"></span><br><span class="line">    replacer_-&gt;<span class="built_in">Pin</span>(frame_id);          <span class="comment">// In usage, 不允许被LRU调度，踢出list_</span></span><br><span class="line">    pages_[frame_id].pin_count_ += <span class="number">1</span>;  <span class="comment">// 引用 page，计数+1</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">auto</span> *page = &amp;pages_[frame_id];</span><br><span class="line">    <span class="keyword">return</span> page;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// not exists, 找一个空闲页然后从磁盘里调进来</span></span><br><span class="line">  <span class="type">frame_id_t</span> frame_id;</span><br><span class="line">  <span class="keyword">if</span> (!free_list_.<span class="built_in">empty</span>()) &#123;</span><br><span class="line">    frame_id = free_list_.<span class="built_in">front</span>();</span><br><span class="line">    free_list_.<span class="built_in">pop_front</span>();</span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> (replacer_-&gt;<span class="built_in">Size</span>() &gt; <span class="number">0</span>) &#123;</span><br><span class="line">    replacer_-&gt;<span class="built_in">Victim</span>(&amp;frame_id);</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">auto</span> *page = &amp;pages_[frame_id];</span><br><span class="line">  <span class="comment">// 在这里持久化 Page</span></span><br><span class="line">  <span class="comment">// 接下来需要复用它，所以页面数据要写回去, 否则旧数据就丢了</span></span><br><span class="line">  <span class="keyword">if</span> (page-&gt;<span class="built_in">IsDirty</span>()) &#123;</span><br><span class="line">    disk_manager_-&gt;<span class="built_in">WritePage</span>(page-&gt;page_id_, page-&gt;data_);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 更新page_table</span></span><br><span class="line">  page_table_.<span class="built_in">erase</span>(page-&gt;page_id_);       <span class="comment">// remove old record</span></span><br><span class="line">  page_table_.<span class="built_in">emplace</span>(page_id, frame_id);  <span class="comment">// add new record</span></span><br><span class="line">  <span class="comment">// page-&gt;ResetMemory();  // 不是很必要, ReadPage时会覆盖掉</span></span><br><span class="line"></span><br><span class="line">  replacer_-&gt;<span class="built_in">Pin</span>(frame_id);</span><br><span class="line">  page-&gt;pin_count_ += <span class="number">1</span>;</span><br><span class="line">  page-&gt;page_id_ = page_id;</span><br><span class="line">  page-&gt;is_dirty_ = <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">  disk_manager_-&gt;<span class="built_in">ReadPage</span>(page_id, page-&gt;data_);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> page;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 不在需要此页，释放内存页</span></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">BufferPoolManagerInstance::DeletePgImp</span><span class="params">(<span class="type">page_id_t</span> page_id)</span> </span>&#123;</span><br><span class="line">  <span class="function">std::lock_guard&lt;std::mutex&gt; <span class="title">lkguard</span><span class="params">(latch_)</span></span>;</span><br><span class="line">  <span class="comment">// Page not exists</span></span><br><span class="line">  <span class="keyword">auto</span> iter = page_table_.<span class="built_in">find</span>(page_id);</span><br><span class="line">  <span class="keyword">if</span> (iter == page_table_.<span class="built_in">end</span>()) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// exists, someone is using the page</span></span><br><span class="line">  <span class="keyword">auto</span> frame_id = iter-&gt;second;</span><br><span class="line">  <span class="keyword">if</span> (pages_[frame_id].pin_count_ != <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  page_table_.<span class="built_in">erase</span>(pages_[frame_id].page_id_);</span><br><span class="line"></span><br><span class="line">  pages_[frame_id].pin_count_ = <span class="number">0</span>;</span><br><span class="line">  pages_[frame_id].page_id_ = INVALID_PAGE_ID;</span><br><span class="line">  pages_[frame_id].is_dirty_ = <span class="literal">false</span>;</span><br><span class="line">  pages_[frame_id].<span class="built_in">ResetMemory</span>();</span><br><span class="line"></span><br><span class="line">  free_list_.<span class="built_in">emplace_back</span>(frame_id);</span><br><span class="line">  <span class="built_in">DeallocatePage</span>(page_id);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 表示此 Page 可以被移除内存(对某个线程来说)</span></span><br><span class="line"><span class="comment">// 当所有线程都不 pin this Page时候, 就可以交给 LRU调度了</span></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">BufferPoolManagerInstance::UnpinPgImp</span><span class="params">(<span class="type">page_id_t</span> page_id, <span class="type">bool</span> is_dirty)</span> </span>&#123;</span><br><span class="line">  <span class="function">std::lock_guard&lt;std::mutex&gt; <span class="title">lkguard</span><span class="params">(latch_)</span></span>;</span><br><span class="line">  <span class="keyword">auto</span> iter = page_table_.<span class="built_in">find</span>(page_id);</span><br><span class="line">  <span class="keyword">if</span> (iter == page_table_.<span class="built_in">end</span>()) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">auto</span> frame_id = iter-&gt;second;</span><br><span class="line">  <span class="keyword">if</span> (pages_[frame_id].pin_count_ &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  pages_[frame_id].pin_count_ -= <span class="number">1</span>; <span class="comment">// 引用计数 -1</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// 大家都不再使用此 page，加入LRU 调度</span></span><br><span class="line">  <span class="keyword">if</span> (pages_[frame_id].pin_count_ == <span class="number">0</span>) &#123;</span><br><span class="line">    replacer_-&gt;<span class="built_in">Unpin</span>(frame_id);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// mark Page&#x27;s is_dirty flag</span></span><br><span class="line">  <span class="comment">// 必须 is_dirty flag为true才标记，不管true/false,无脑标记是通不过gradescope测试的</span></span><br><span class="line">   <span class="keyword">if</span> (is_dirty) &#123;</span><br><span class="line">     pages_[frame_id].is_dirty_ = <span class="literal">true</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h1 id="Task-3-Parallel-Buffer-Pool-Manager"><a href="#Task-3-Parallel-Buffer-Pool-Manager" class="headerlink" title="Task 3: Parallel Buffer Pool Manager"></a>Task 3: Parallel Buffer Pool Manager</h1><h2 id="parallel-buffer-pool-manager-h"><a href="#parallel-buffer-pool-manager-h" class="headerlink" title="parallel_buffer_pool_manager.h"></a>parallel_buffer_pool_manager.h</h2><p>此处的 <code>.h</code> 文件时需要补充私有成员的，可以对照着<code>.cpp</code>文件的构造和题目的描述来添加对应的成员。核心成员就是那个<code>bpms_</code>（用来管理所有的BufferPoool）</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span>:</span><br><span class="line">  <span class="comment">/** How many instances are in the parallel BPM (if present, otherwise just 1 BPI) */</span></span><br><span class="line">  <span class="type">const</span> <span class="type">uint32_t</span> num_instances_;</span><br><span class="line">  <span class="comment">/** Number of pages in the buffer pool. */</span></span><br><span class="line">  <span class="type">const</span> <span class="type">size_t</span> pool_size_;</span><br><span class="line">  <span class="comment">/** Start index */</span></span><br><span class="line">  <span class="type">size_t</span> start_index_ = <span class="number">0</span>;</span><br><span class="line">  <span class="comment">/** Array of BPMs*/</span></span><br><span class="line">  BufferPoolManager **bpms_;</span><br><span class="line">  <span class="comment">/** Pointer to the disk manager. */</span></span><br><span class="line">  DiskManager *disk_manager_ __attribute__((__unused__));</span><br><span class="line">  <span class="comment">/** Pointer to the log manager. */</span></span><br><span class="line">  LogManager *log_manager_ __attribute__((__unused__));</span><br></pre></td></tr></table></figure>

<h2 id="parallel-buffer-pool-manager-cpp"><a href="#parallel-buffer-pool-manager-cpp" class="headerlink" title="parallel_buffer_pool_manager.cpp"></a>parallel_buffer_pool_manager.cpp</h2><p>多了一个mod取余操作分散任务给不同的bpm，进而减少锁竞争。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">ParallelBufferPoolManager::<span class="built_in">ParallelBufferPoolManager</span>(<span class="type">size_t</span> num_instances, <span class="type">size_t</span> pool_size, DiskManager *disk_manager, LogManager *log_manager)</span><br><span class="line">    : <span class="built_in">num_instances_</span>(num_instances), <span class="built_in">pool_size_</span>(pool_size), <span class="built_in">disk_manager_</span>(disk_manager), <span class="built_in">log_manager_</span>(log_manager)&#123;</span><br><span class="line">  <span class="comment">// Allocate and create individual BufferPoolManagerInstances</span></span><br><span class="line">  bpms_ = <span class="built_in">static_cast</span>&lt;BufferPoolManager **&gt;(<span class="keyword">operator</span> <span class="keyword">new</span>[](<span class="built_in">sizeof</span>(BufferPoolManager *) * num_instances_));</span><br><span class="line"></span><br><span class="line">  <span class="keyword">for</span> (<span class="type">size_t</span> i = <span class="number">0</span>; i &lt; num_instances_; ++i) &#123;</span><br><span class="line">    bpms_[i] = <span class="keyword">new</span> <span class="built_in">BufferPoolManagerInstance</span>(pool_size_, num_instances_, i, disk_manager_, log_manager_);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 析构函数，注意按照申请内存的反方向顺序析构</span></span><br><span class="line">ParallelBufferPoolManager::~<span class="built_in">ParallelBufferPoolManager</span>() &#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="type">size_t</span> i = <span class="number">0</span>; i &lt; num_instances_; ++i) &#123;</span><br><span class="line">    <span class="keyword">delete</span> bpms_[i];</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">delete</span>[] bpms_;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">size_t</span> <span class="title">ParallelBufferPoolManager::GetPoolSize</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> pool_size_ * num_instances_;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 这里使用 mod取余，将任务分散给不同的bpm处理</span></span><br><span class="line"><span class="function">BufferPoolManager *<span class="title">ParallelBufferPoolManager::GetBufferPoolManager</span><span class="params">(<span class="type">page_id_t</span> page_id)</span> </span>&#123;</span><br><span class="line">  <span class="comment">// Get BufferPoolManager responsible for handling given page id. You can use this method in your other methods.</span></span><br><span class="line">  <span class="keyword">return</span> bpms_[page_id % num_instances_];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function">Page *<span class="title">ParallelBufferPoolManager::FetchPgImp</span><span class="params">(<span class="type">page_id_t</span> page_id)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">auto</span> *bpm = <span class="built_in">GetBufferPoolManager</span>(page_id);</span><br><span class="line">  <span class="keyword">return</span> bpm-&gt;<span class="built_in">FetchPage</span>(page_id);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">ParallelBufferPoolManager::UnpinPgImp</span><span class="params">(<span class="type">page_id_t</span> page_id, <span class="type">bool</span> is_dirty)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">auto</span> *bpm = <span class="built_in">GetBufferPoolManager</span>(page_id);</span><br><span class="line">  <span class="keyword">return</span> bpm-&gt;<span class="built_in">UnpinPage</span>(page_id, is_dirty);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">ParallelBufferPoolManager::FlushPgImp</span><span class="params">(<span class="type">page_id_t</span> page_id)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">auto</span> *bpm = <span class="built_in">GetBufferPoolManager</span>(page_id);</span><br><span class="line">  <span class="keyword">return</span> bpm-&gt;<span class="built_in">FlushPage</span>(page_id);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function">Page *<span class="title">ParallelBufferPoolManager::NewPgImp</span><span class="params">(<span class="type">page_id_t</span> *page_id)</span> </span>&#123;</span><br><span class="line">  Page *page;</span><br><span class="line">  <span class="keyword">for</span> (<span class="type">size_t</span> i = <span class="number">0</span>; i &lt; num_instances_; i++) &#123;</span><br><span class="line">    <span class="keyword">auto</span> *bpm = <span class="built_in">GetBufferPoolManager</span>(start_index_++);</span><br><span class="line">    page = bpm-&gt;<span class="built_in">NewPage</span>(page_id);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (page != <span class="literal">nullptr</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> page;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">ParallelBufferPoolManager::DeletePgImp</span><span class="params">(<span class="type">page_id_t</span> page_id)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">auto</span> *bpm = <span class="built_in">GetBufferPoolManager</span>(page_id);</span><br><span class="line">  <span class="keyword">return</span> bpm-&gt;<span class="built_in">DeletePage</span>(page_id);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">ParallelBufferPoolManager::FlushAllPgsImp</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="type">size_t</span> i = <span class="number">0</span>; i &lt; num_instances_; ++i) &#123;</span><br><span class="line">    bpms_[i]-&gt;<span class="built_in">FlushAllPages</span>();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个项目其实几天前已经完成了，并且通过gradescope oj ，因为中间有开题报告，文本分析等事情耽搁了，文章也就一直没写，今晚动笔补上。</p>
<h1 id="Googletest"><a href="#Googletest" class="headerlink" title="Googletest"></a>Googletest</h1><p><img  
                     lazyload
                     src="/images/loading.svg"
                     data-src="/../img/cmu15445_p1_gtest.png"
                      alt="gtest"
                ></p>
<h1 id="Gradescope"><a href="#Gradescope" class="headerlink" title="Gradescope"></a>Gradescope</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">make format</span><br><span class="line">make check-lint</span><br><span class="line">make check-clang-tidy</span><br><span class="line"><span class="built_in">cd</span> ..</span><br><span class="line">zip project1-submission.zip \</span><br><span class="line">    src/include/buffer/lru_replacer.h \</span><br><span class="line">    src/buffer/lru_replacer.cpp \</span><br><span class="line">    src/include/buffer/buffer_pool_manager_instance.h \</span><br><span class="line">    src/buffer/buffer_pool_manager_instance.cpp \</span><br><span class="line">    src/include/buffer/parallel_buffer_pool_manager.h \</span><br><span class="line">    src/buffer/parallel_buffer_pool_manager.cpp</span><br></pre></td></tr></table></figure>



<p><img  
                     lazyload
                     src="/images/loading.svg"
                     data-src="/../img/cmu15445_p1_gradescope.png"
                      alt="gradescope"
                ></p>
]]></content>
      <categories>
        <category>cmu15445</category>
      </categories>
      <tags>
        <tag>cmu15445</tag>
        <tag>project1</tag>
        <tag>buffer pool</tag>
      </tags>
  </entry>
  <entry>
    <title>Hexo Next主题配置（图片配置）</title>
    <url>/2022/01/02/Hexo-Next%E4%B8%BB%E9%A2%98%E9%85%8D%E7%BD%AE/</url>
    <content><![CDATA[<h1 id="一、图片配置"><a href="#一、图片配置" class="headerlink" title="一、图片配置"></a>一、图片配置</h1><p>hexo 本地引入图片无法被markdown渲染，导致本地看不见，云端经过hexo 渲染后看得见。其实就是个路径问题。</p>
<p><strong>修改引入图片路径即可解决，</strong></p>
<p>我们只需要在 <code>_posts</code> 的同级目录下新建一个 <code>img</code> 文件夹专门用来存放所有的图片，markdown里面引用使用相对格式</p>
<p><code>../img/gris.png</code> (后面有效果展示)。其实非常简单，引用的时候加两个点即可，这样本地和云端都可以正常预览&#x2F;显示图片，<strong>但是文件名不要有空格</strong>，有空格云端显示不出来。</p>
<p><img  
                     lazyload
                     src="/images/loading.svg"
                     data-src="/../img/gris.png"
                      alt="Gris"
                ></p>
<p><strong>结构展示</strong></p>
<p>如果没有理解的话，看看这张结构图</p>
<p><img  
                     lazyload
                     src="/images/loading.svg"
                     data-src="/../img/dict_tree.png"
                     
                ></p>
<h1 id="二、插件"><a href="#二、插件" class="headerlink" title="二、插件"></a>二、插件</h1><p>安装插件的时候要多搜索，寻找最近的文章看，如果你也是使用Next 主题，尽量找Hexo Next 官方最近更新的文档看，因为随着<code>Hexo/Next</code>的版本更新，很多功能都被开发者集成进去了，只需要修改一下主题配置文件即可，<strong>简而言之，找最新的文章&#x2F;文档参考。</strong></p>
<blockquote>
<p>Hexo-Next 文档地址</p>
<p>Hexo-Next: <a class="link"   href="https://hexo-next.readthedocs.io/zh_CN/latest" >https://hexo-next.readthedocs.io/zh_CN/latest<i class="fas fa-external-link-alt"></i></a></p>
</blockquote>
<h2 id="安装的插件"><a href="#安装的插件" class="headerlink" title="安装的插件"></a>安装的插件</h2><p>安装的插件并不多，主要是够用即可</p>
<p>hexo-symbols-count-time</p>
<p>hexo-excerpt —— Because, auto_excerpt not working</p>
<p>hexo-generator-searchdb——站内搜索</p>
<p>大家都喜欢的插件，Next 文档都已经列出来了，去找就行了🤣</p>
<blockquote>
<p>Hexo-Next: <a class="link"   href="https://hexo-next.readthedocs.io/zh_CN/latest" >https://hexo-next.readthedocs.io/zh_CN/latest<i class="fas fa-external-link-alt"></i></a></p>
</blockquote>
<p><img  
                     lazyload
                     src="/images/loading.svg"
                     data-src="/../img/Next_dos_advanced.png"
                     
                ></p>
]]></content>
      <categories>
        <category>hexo</category>
      </categories>
      <tags>
        <tag>hexo</tag>
      </tags>
  </entry>
</search>
